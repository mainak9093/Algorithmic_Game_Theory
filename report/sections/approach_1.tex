%% ==========================================================================
%%  4  Approach 1
%%
%%  TEMPLATE for an approach section.  One approach = one file = one section.
%%  Copy this file to approach_2.tex, approach_3.tex, ... as further
%%  approaches arrive, and add the matching \input line in main.tex.
%%
%%  Suggested shape, following R3's Section 4:
%%    - one paragraph saying what the approach does and why it should work
%%    - the construction / algorithm
%%    - the invariant it maintains
%%    - the lemmas, then the proof of the main statement
%%    - if the approach fails, say so and say exactly where it breaks --- a
%%      recorded dead end is a result and belongs in the paper, not in a
%%      deleted file.  Cross-reference it from Notes_and_Findings/.
%% ==========================================================================
\section{Approach 1}
\label{sec:approach1}

\todo{awaiting content.}
