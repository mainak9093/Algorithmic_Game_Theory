%% ==========================================================================
%%  3  Our Results
%%
%%  INTENTIONALLY EMPTY.  Nothing goes here until an approach has actually
%%  produced a theorem.
%%
%%  When it does, this section should hold the STATEMENTS only --- the main
%%  theorem, its corollaries on total subsidy, and the tightness claim
%%  pointing back at Example~\ref{ex:lowerbound}.  The proofs live in the approach
%%  section that produced them (sections/approach_<k>.tex), or in an
%%  appendix, exactly as R3 states Theorem 4 in its Section 3 and proves it
%%  in its Section 4.
%%
%%  Environments available (all share one counter, as in R3):
%%    theorem, lemma, proposition, corollary, definition,
%%    observation, conjecture, example, remark
%%  plus claim, which carries its own counter.
%% ==========================================================================
\section{Our Results}
\label{sec:results}

\todo{empty by design --- fill in once Approach~1 has produced a theorem.
The statement being aimed at is the negative dichotomous analogue
of~\cite[Theorem~4]{BKNS22}: an envy-free solution $(\alloc, \subsidy)$ with
$\subsidy \in \set{0,1}^n$, hence total subsidy at most $n-1$, computable in
polynomial time given value-oracle access. Example~\ref{ex:lowerbound} already
supplies the matching lower bound.}
